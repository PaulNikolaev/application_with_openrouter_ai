# Core application framework for building desktop and mobile applications
flet>=0.80.2

# Flet desktop client (required for desktop builds)
flet-desktop>=0.80.2

# Environment variables management - loads .env files for configuration
python-dotenv>=1.0.0

# Desktop build tool - converts Python application to executable (Windows/Linux/macOS only)
pyinstaller==6.11.1

# HTTP library for making API requests to OpenRouter service
requests>=2.28.0

# System and process utilities for performance monitoring (desktop only)
psutil>=5.9.0